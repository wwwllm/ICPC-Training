---
tags:
  - 数据结构
  - 线段树
Time: 2026-03-06T12:01:00
---

# 定义

**权值线段树**（Value Segment Tree）是线段树的一种变体。
* **普通线段树**的节点维护的是**数组下标**区间 $[l, r)$ 的统计信息（如区间和、最值）。
* **权值线段树**的节点维护的是**数值（即权值区间 $[l, r)$** 的统计信息，通常是该值域范围内数字出现的**频数（次数）**。


它的核心优势在于能够在 $\mathcal{O}(\log V)$（$V$ 为值域大小）的时间复杂度内，高效解决全局第 $k$ 小/大、查询元素排名等问题，是进阶数据结构（如可持久化线段树/主席树）的基础。

## 实现
权值线段树的核心操作主要基于线段树的二分性质：

1. **单点修改 (`modify`)**：
   在插入或删除某个数字 $x$ 时，相当于在值域线段树中，将叶子节点 $x$ 对应的频数加 $1$ 或减 $1$。然后自底向上更新父节点的频数总和。

2. **查询全局第 $k$ 小 (`kth`)**：
   * 从根节点开始，假设当前节点维护的值域为 $[l, r)$，左子树维护 $[l, mid)$，其频数和为 $left\_cnt$。
   * 如果 $k \le left\_cnt$，说明第 $k$ 小的数落在左半值域，向左子树递归。
   * 如果 $k > left\_cnt$，说明左半值域的数不够，第 $k$ 小的数在右半值域。此时向右子树递归，**但要将 $k$ 减去 $left\_cnt$**（即在右半部分寻找第 $k - left\_cnt$ 小的数）。
   * 当递归到叶子节点（即 $r - l == 1$）时，当前的 $l$ 就是我们要找的数值。

如果题目的数值范围极大（如 $10^9$）或包含负数，直接建树会内存超限（MLE）。此时必须配合**离散化**（将大数值映射到 $1 \dots N$ 的连续排名），或者使用**动态开点**技术。
 
### 代码

以下是基于半开半闭区间 `[l, r)` 且无懒标记的权值线段树模板（适用于较小值域或已离散化后的数组）：

```cpp
#include <bits/stdc++.h>
#define int long long
using namespace std;

const int inf = 1e9;
struct Info
{
    int cnt;
    Info() : cnt(0){};
    Info(int value) :cnt(value) {};
    Info operator+(const Info &other) const
    {
        return Info(cnt+other.cnt);
    }
};
struct ValueSegTree
{
    int n;
    vector<int> tag;
    vector<Info> info;
    ValueSegTree(int _n) : n(_n), tag(4 * _n), info(4 * n) {};
    void pull(int p) // 更新上移
    {
        info[p] = info[p << 1] + info[p << 1 | 1];
    }
    // 区间查询[x,y)
    Info query(int p, int l, int r, int x, int y)
    {
        if (l >= y || r <= x)
            return {};
        if (l >= x && r <= y)
        {
            return info[p];
        }
        int mid = l + r >> 1;
        return query(p << 1, l, mid, x, y) + query(p << 1 | 1, mid, r, x, y);
    };
    int query(int x, int y)
    {
        return query(1, 0, n, x, y).cnt;
    };

    int rank(int val){
        return query(0,val)+1;
    }
    
    // 单点修改:在x上加v
    void modify(int p, int l, int r, int x, int v)
    {
        if (r - l == 1)
        {
            info[p].cnt += v;
            return;
        }
        int mid = l + r >> 1;
        if (x < mid)
        {
            modify(p << 1, l, mid, x, v);
        }
        else
        {
            modify(p << 1 | 1, mid, r, x, v);
        }
        pull(p);
    }
    void modify(int x, int v)
    {
        modify(1, 0, n, x, v);
    }

    int query_kth(int p,int l,int r,int k){
        if(r-l==1){
            return l;
        }
        int mid=l+r>>1;
        int l_cnt=info[p<<1].cnt;
        if(k<=l_cnt)
            return query_kth(p<<1,l,mid,k);
        else 
            return query_kth(p<<1|1,mid,r,k-l_cnt);
    }
    int query_kth(int k){
        return query_kth(1,0,n,k);
    }
};
void solve()
{
    int n;
    cin>>n;
    vector<array<int,2>>q(n);
    vector<int>b;
    for(auto &[op,v]:q){
        cin>>op>>v;
        b.push_back(v);
    }
    ranges::sort(b);
    b.erase(unique(b.begin(),b.end()),b.end());
    auto get_idx=[&](int x){
        return ranges::lower_bound(b,x)-b.begin()+1;
    };
    ValueSegTree T((int)b.size()+1);
    
    for(auto [op,v]:q){
        int idx=get_idx(v);
        if(op==1){
            T.modify(idx,1);
        }
        else if(op==2){
            T.modify(idx,-1);
        }
        else if(op==3){
            cout<<T.rank(idx)<<'\n';
        }
        else if(op==4){
            cout<<b[T.query_kth(v)-1]<<'\n';
        }
        else if(op==5){
            cout<<b[T.query_kth(T.rank(idx)-1)-1]<<'\n';
        }
        else{
            cout<<b[T.query_kth(T.query(0,idx+1)+1)-1]<<'\n';
        }
    }
}
signed main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int t = 1;
    // cin >> t;
    while (t--)
        solve();
    return 0;
}

```

### 例题链接

[权值线段树](https://www.luogu.com.cn/problem/P3369)

# 动态开点权值线段树

对于普通的权值线段树，直接开 $4*n$ 的数组可能会爆内存。采取动态开点线段树就可以解决这个问题。

## 实现

一开始，我们只有一个空荡荡的根节点。只有当我们要修改某个位置（走过某条路径）时，如果发现前面的节点还没建出来，我们就**临时给它分配一个编号**。

### 代码

```cpp
struct SegTree{
    struct Node{
        int l;
        int r;
        int cnt;
    };
    int root;
    int tot;
    int n;
    vector<Node>info;
    SegTree(int n,int cnt):n(n),root(0),tot(0){
        info.assign(cnt,{0,0,0});
        tot=0;
        root=0;
    }
    void pull(int p){
        int l=info[p].l,r=info[p].r;
        info[p].cnt=info[l].cnt+info[r].cnt;
    }

    void modify(int &p,int l,int r,int x,int v){
        if(!p)p=++tot;
        if(r-l==1){
            info[p].cnt+=v;
            return ;
        }
        int mid=l+r>>1;
        if(x<mid){
            modify(info[p].l,l,mid,x,v);
        }
        else{
            modify(info[p].r,mid,r,x,v);
        }
        pull(p);
    }
    void modify(int x,int v){
        return modify(root,0,n,x,v);
    }

    int query(int p,int l,int r,int x,int y){
        if(!p||l>=y||r<=x){
            return 0;
        }
        if(l>=x&&r<=y){
            return info[p].cnt;
        }
        int mid=l+r>>1;
        return query(info[p].l,l,mid,x,y)+query(info[p].r,mid,r,x,y);
    }
    int query(int x,int y){
        return query(root,0,n,x,y);
    }
    int rank(int val){
        return query(0,val)+1;
    }
    int kth(int p,int l,int r,int k){
        if(r-l==1)
            return l;
        int mid=l+r>>1;
        int cntl=info[info[p].l].cnt;
        int cntr=info[info[p].r].cnt;
        if(k<=cntl){
            return kth(info[p].l,l,mid,k);
        }
        else {
            return kth(info[p].r,mid,r,k-cntl);
        }
    }
    int kth(int k){
        return kth(root,0,n,k);
    }
};
```
### 例题链接

[权值线段树](https://www.luogu.com.cn/problem/P3369)