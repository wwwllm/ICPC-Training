#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, op;
    cin >> n;
    cout << "mul 9" << endl;
    cin >> op;
    cout << "digit" << endl;
    cin >> op;
    cout << "digit" << endl;
    cin >> op;
    cout << "add " << n - 9 << endl;
    cin >> op;
    cout << "!" << endl;
    cin >> op;
}
signed main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}