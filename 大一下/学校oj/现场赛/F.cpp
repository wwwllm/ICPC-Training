#include <bits/stdc++.h>
using namespace std;

// dp[i][j]：表示匹配到第i位和第j位
// dp[i][j]=max(dp[i-1][j],dp[i][j-1],dp[i-1][j-1]+a[i]*b[i]);
// dp[0][0]=0;
void solve()
{
    
}
signed main()
{
    ios::sync_with_stdio(0), cout.tie(0), cin.tie(0);
    int t = 1;
    // cin>>t;
    while (t--)
        solve();
    return 0;
}